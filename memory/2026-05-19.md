- Vibe Zone YouTube Automation/Dog Content loop fix: Rex changed `src/App.tsx` so Dog Content project tiles are discovered from local `media/practice/youtube-automation/dog-content/<project>/...` assets instead of being hardcoded to only Dog Paw/Dog Staring. Added three audio-ready researched voiceover scripts plus markdown/source notes under Vibe Zone: `why-dogs-follow-you-everywhere`, `why-dogs-get-zoomies`, and `why-dogs-look-guilty`. Verification: `/api/state` now lists the three new `script-v1-voiceover-only.txt` files, `npm run build` passed, `npm run lint` passed, and `graphify update .` refreshed the graph.
- After Masala challenged the new Dog Content scripts as too short, Rex re-did the remaining scripts properly against the Dog Content SOP. Rewrote `why-dogs-follow-you-everywhere`, `why-dogs-get-zoomies`, and `why-dogs-look-guilty` to SOP-length audio-ready drafts (1642, 1630, 1609 words), added SEO files and visual-plan stubs, migrated Dog Staring into the same project-folder convention, and added `scripts/check-dog-content-sop.mjs` plus `npm run dog:sop` so Dog Content project folders fail if voiceover text is under 1600 words or missing script/source-notes/SEO/visual-plan files. Verification: `npm run build`, `npm run lint`, `npm run dog:sop`, `/api/state` script visibility, and `graphify update .` all passed.
- Masala reinforced that Dog Content must process through the SOP, including end CTAs that lead into the next video. Rex added `vibe-zone/DOG_CONTENT_SOP_CHECKLIST.md`, updated all current Dog Content scripts with explicit next-video bridges (Dog Staring → Follow Everywhere → Zoomies → Guilty Look → Bring Toys/Gifts), and expanded `scripts/check-dog-content-sop.mjs` so `npm run dog:sop` now enforces word count, required assets, writing formula checks (owner-recognition hook, common assumption, curiosity turn, research-backed explanation, body/context nuance, practical takeaway, emotional payoff), and CTA chain. Verification passed: `npm run build`, `npm run lint`, `npm run dog:sop`, API visibility check, and `graphify update .`.
- Dog Staring full video status: all 132 approved frames existed under `youtube-automation/projects/dog-staring-psychology/full_video_prep/approved_frames`. Rex stitched the clean full preview with the 695.6s voiceover to `youtube-automation/projects/dog-staring-psychology/full_video_prep/dog_staring_full_preview.mp4` and copied it to Vibe Zone at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-full-preview.mp4`. Size ~152 MB, duration 11:35.6. Proof frames extracted at 00:30, 05:45, and 11:20. This is the clean stitched preview; subtitle/caption QA is separate if needed.
- Masala watched the Dog Staring stitched preview and caught two production failures: frames did not reliably match the audio, and captions were missing. Permanent fix: Rex created an audio-truth pipeline where final voiceover Whisper word timestamps drive both frame durations and one-line captions. Added reusable script `youtube-automation/scripts/render_dog_audio_truth_video.mjs` and doc `youtube-automation/docs/DOG_CONTENT_AUDIO_TRUTH_PIPELINE.md`. Regenerated Dog Staring as `youtube-automation/projects/dog-staring-psychology/full_video_prep/dog_staring_audio_truth_captioned.mp4` and copied to Vibe Zone at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-audio-truth-captioned.mp4`. Audit: 132 beats, 348 captions, plan/audio wordRatio 1.0016, duration 695.6s. Vibe Zone now prefers `audio-truth-captioned.mp4` files for Dog Content review. Verification passed: proof frames show visible captions, `npm run build`, `npm run lint`, `/api/state` sees the captioned copy, and `graphify update .`.
- Masala reported Dog Staring captions still felt wrong / per-image rather than per-audio and asked for a bulletproof cron/job order. Root cause acknowledged: previous “audio-truth” render still allowed weak proportional frame timing and older/debug renders remained visible. Rex patched `youtube-automation/scripts/render_dog_audio_truth_video.mjs` so visual beats now align by exact token walk against Whisper word timestamps and fail on low alignment or caption counts that look beat/image-based. Regenerated `dog_staring_audio_truth_captioned_v2.mp4`; audit: 132 beats, 348 captions, wordRatio 1.0016, timingMethod `exact-token-walk-from-whisper-word-timestamps`, avgBeatAlignmentScore 0.9721, 7 weak beats. Vibe Zone now exposes only `dog-staring-audio-truth-captioned-v2.mp4` for Dog Staring review; older clean/first captioned versions moved to `_debug_archive/dog-staring-20260519`. Added mandatory job-order doc `youtube-automation/docs/DOG_CONTENT_PRODUCTION_JOB_ORDER.md`. Verification passed: Vibe build/lint, `/api/state` shows only the v2 review MP4, proof frames show visible captions, `npm run dog:sop`, and `graphify update .`.
- Masala asked Rex to create all possible videos from the three new Dog Content audio files plus 5–10 thumbnail options. Result: all 3 follow-up videos were produced as audio-truth captioned review files in Vibe Zone: `why-dogs-follow-you-everywhere/why-dogs-follow-you-everywhere-audio-truth-captioned.mp4` (114 beats, 333 captions, wordRatio 1, avg alignment 1), `why-dogs-get-zoomies/why-dogs-get-zoomies-audio-truth-captioned.mp4` (117 beats, 354 captions, wordRatio 1, avg alignment 1), and `why-dogs-look-guilty/why-dogs-look-guilty-audio-truth-captioned.mp4` (108 beats, 324 captions, wordRatio 1, avg alignment 1). Created 9 thumbnail options per video under each project's `thumbnails/thumbnail_option_01.png` through `_09.png`; cropped batch outputs into individual 16:9 options and replaced a bad Zoomies option with clean `NOT RANDOM` text. Verification passed: proof-frame visual QA with visible captions, thumbnail sample QA, `npm run build`, `npm run lint`, `npm run dog:sop`, `/api/state` sees each final video and 9 thumbnails per project, and `graphify update .`.
- Masala was understanding about mistakes and asked Rex to update the core Dog Content pipeline so future runs automatically avoid the same placeholder-frame failure. Rex added `youtube-automation/scripts/check_dog_frame_quality.mjs`, patched `scripts/render_dog_audio_truth_video.mjs` to block renders unless `approved_frames/` pass the real image-generation frame gate, updated `DOG_CONTENT_PRODUCTION_JOB_ORDER.md` and `DOG_CONTENT_AUDIO_TRUTH_PIPELINE.md`, added Vibe Zone `npm run dog:production`, and changed the follow-up project statuses to `needs real frames`. Rex moved the three placeholder/vector audio-truth videos and 1-minute previews out of the visible Vibe Zone media lane into `vibe-zone/_storyboard_archive/dog-content-placeholder-videos-20260519/`; Vibe Zone now exposes only the real-frame Dog Staring review video. Dog Staring received a `frame_generation_manifest.json` and passes frame quality; placeholder projects fail the new gate as intended. Verification passed: `npm run build`, `npm run lint`, `npm run dog:production`, `/api/state` shows only Dog Staring MP4 in Dog Content review, and `graphify update .`.
- Status check after real-frame run: the 3 follow-up videos had real OpenAI generated frames but only 8–9 unique source frames reused across 108–117 beat filenames. Rex tightened `check_dog_frame_quality.mjs` so “complete” now requires enough unique real generated frames (>=75% of beat count, min 20) and rejects `sourceFrameReuse`. `npm run dog:production` now correctly fails the three follow-ups until unique-frame generation is complete. Rex moved the reused-real-frame videos out of the visible Vibe Zone review lane into `vibe-zone/_review_hold/reused-real-frame-videos-20260519/`, leaving only Dog Staring visible as production-ready. Rex spawned background subagent `dog-content-unique-frame-completion` and added cron job `2c0e3836-e024-4160-a5a2-27f82e1cf43a` every 30 minutes to continue/verify unique-frame completion without owner input until `npm run dog:production` passes for all four videos.
- Masala approved the Dog Content examples and asked to also make Dog Staring match the newer perfect workflow. Rex rerendered Dog Staring through `render_dog_audio_truth_video.mjs` using `visual_plan_v2_132.json` + final voiceover alignment, producing a clean master with `mov_text` soft subtitle track at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-audio-truth-captioned-v2.mp4` and Telegram-visible review cut `.../dog-staring-psychology/previews/dog-staring-preview-1min-telegram-visible-captions.mp4`. Verification: master streams include h264 video + aac audio + mov_text subtitle; `npm run dog:production` passes all four current Dog Content videos.
- Vibe Zone YouTube Automation/Dog Content loop fix: Rex changed `src/App.tsx` so Dog Content project tiles are discovered from local `media/practice/youtube-automation/dog-content/<project>/...` assets instead of being hardcoded to only Dog Paw/Dog Staring. Added three audio-ready researched voiceover scripts plus markdown/source notes under Vibe Zone: `why-dogs-follow-you-everywhere`, `why-dogs-get-zoomies`, and `why-dogs-look-guilty`. Verification: `/api/state` now lists the three new `script-v1-voiceover-only.txt` files, `npm run build` passed, `npm run lint` passed, and `graphify update .` refreshed the graph.
- After Masala challenged the new Dog Content scripts as too short, Rex re-did the remaining scripts properly against the Dog Content SOP. Rewrote `why-dogs-follow-you-everywhere`, `why-dogs-get-zoomies`, and `why-dogs-look-guilty` to SOP-length audio-ready drafts (1642, 1630, 1609 words), added SEO files and visual-plan stubs, migrated Dog Staring into the same project-folder convention, and added `scripts/check-dog-content-sop.mjs` plus `npm run dog:sop` so Dog Content project folders fail if voiceover text is under 1600 words or missing script/source-notes/SEO/visual-plan files. Verification: `npm run build`, `npm run lint`, `npm run dog:sop`, `/api/state` script visibility, and `graphify update .` all passed.
- Masala reinforced that Dog Content must process through the SOP, including end CTAs that lead into the next video. Rex added `vibe-zone/DOG_CONTENT_SOP_CHECKLIST.md`, updated all current Dog Content scripts with explicit next-video bridges (Dog Staring → Follow Everywhere → Zoomies → Guilty Look → Bring Toys/Gifts), and expanded `scripts/check-dog-content-sop.mjs` so `npm run dog:sop` now enforces word count, required assets, writing formula checks (owner-recognition hook, common assumption, curiosity turn, research-backed explanation, body/context nuance, practical takeaway, emotional payoff), and CTA chain. Verification passed: `npm run build`, `npm run lint`, `npm run dog:sop`, API visibility check, and `graphify update .`.
- Dog Staring full video status: all 132 approved frames existed under `youtube-automation/projects/dog-staring-psychology/full_video_prep/approved_frames`. Rex stitched the clean full preview with the 695.6s voiceover to `youtube-automation/projects/dog-staring-psychology/full_video_prep/dog_staring_full_preview.mp4` and copied it to Vibe Zone at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-full-preview.mp4`. Size ~152 MB, duration 11:35.6. Proof frames extracted at 00:30, 05:45, and 11:20. This is the clean stitched preview; subtitle/caption QA is separate if needed.
- Masala watched the Dog Staring stitched preview and caught two production failures: frames did not reliably match the audio, and captions were missing. Permanent fix: Rex created an audio-truth pipeline where final voiceover Whisper word timestamps drive both frame durations and one-line captions. Added reusable script `youtube-automation/scripts/render_dog_audio_truth_video.mjs` and doc `youtube-automation/docs/DOG_CONTENT_AUDIO_TRUTH_PIPELINE.md`. Regenerated Dog Staring as `youtube-automation/projects/dog-staring-psychology/full_video_prep/dog_staring_audio_truth_captioned.mp4` and copied to Vibe Zone at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-audio-truth-captioned.mp4`. Audit: 132 beats, 348 captions, plan/audio wordRatio 1.0016, duration 695.6s. Vibe Zone now prefers `audio-truth-captioned.mp4` files for Dog Content review. Verification passed: proof frames show visible captions, `npm run build`, `npm run lint`, `/api/state` sees the captioned copy, and `graphify update .`.
- Masala reported Dog Staring captions still felt wrong / per-image rather than per-audio and asked for a bulletproof cron/job order. Root cause acknowledged: previous “audio-truth” render still allowed weak proportional frame timing and older/debug renders remained visible. Rex patched `youtube-automation/scripts/render_dog_audio_truth_video.mjs` so visual beats now align by exact token walk against Whisper word timestamps and fail on low alignment or caption counts that look beat/image-based. Regenerated `dog_staring_audio_truth_captioned_v2.mp4`; audit: 132 beats, 348 captions, wordRatio 1.0016, timingMethod `exact-token-walk-from-whisper-word-timestamps`, avgBeatAlignmentScore 0.9721, 7 weak beats. Vibe Zone now exposes only `dog-staring-audio-truth-captioned-v2.mp4` for Dog Staring review; older clean/first captioned versions moved to `_debug_archive/dog-staring-20260519`. Added mandatory job-order doc `youtube-automation/docs/DOG_CONTENT_PRODUCTION_JOB_ORDER.md`. Verification passed: Vibe build/lint, `/api/state` shows only the v2 review MP4, proof frames show visible captions, `npm run dog:sop`, and `graphify update .`.
- Masala asked Rex to create all possible videos from the three new Dog Content audio files plus 5–10 thumbnail options. Result: all 3 follow-up videos were produced as audio-truth captioned review files in Vibe Zone: `why-dogs-follow-you-everywhere/why-dogs-follow-you-everywhere-audio-truth-captioned.mp4` (114 beats, 333 captions, wordRatio 1, avg alignment 1), `why-dogs-get-zoomies/why-dogs-get-zoomies-audio-truth-captioned.mp4` (117 beats, 354 captions, wordRatio 1, avg alignment 1), and `why-dogs-look-guilty/why-dogs-look-guilty-audio-truth-captioned.mp4` (108 beats, 324 captions, wordRatio 1, avg alignment 1). Created 9 thumbnail options per video under each project's `thumbnails/thumbnail_option_01.png` through `_09.png`; cropped batch outputs into individual 16:9 options and replaced a bad Zoomies option with clean `NOT RANDOM` text. Verification passed: proof-frame visual QA with visible captions, thumbnail sample QA, `npm run build`, `npm run lint`, `npm run dog:sop`, `/api/state` sees each final video and 9 thumbnails per project, and `graphify update .`.
- Masala was understanding about mistakes and asked Rex to update the core Dog Content pipeline so future runs automatically avoid the same placeholder-frame failure. Rex added `youtube-automation/scripts/check_dog_frame_quality.mjs`, patched `scripts/render_dog_audio_truth_video.mjs` to block renders unless `approved_frames/` pass the real image-generation frame gate, updated `DOG_CONTENT_PRODUCTION_JOB_ORDER.md` and `DOG_CONTENT_AUDIO_TRUTH_PIPELINE.md`, added Vibe Zone `npm run dog:production`, and changed the follow-up project statuses to `needs real frames`. Rex moved the three placeholder/vector audio-truth videos and 1-minute previews out of the visible Vibe Zone media lane into `vibe-zone/_storyboard_archive/dog-content-placeholder-videos-20260519/`; Vibe Zone now exposes only the real-frame Dog Staring review video. Dog Staring received a `frame_generation_manifest.json` and passes frame quality; placeholder projects fail the new gate as intended. Verification passed: `npm run build`, `npm run lint`, `npm run dog:production`, `/api/state` shows only Dog Staring MP4 in Dog Content review, and `graphify update .`.
- Status check after real-frame run: the 3 follow-up videos had real OpenAI generated frames but only 8–9 unique source frames reused across 108–117 beat filenames. Rex tightened `check_dog_frame_quality.mjs` so “complete” now requires enough unique real generated frames (>=75% of beat count, min 20) and rejects `sourceFrameReuse`. `npm run dog:production` now correctly fails the three follow-ups until unique-frame generation is complete. Rex moved the reused-real-frame videos out of the visible Vibe Zone review lane into `vibe-zone/_review_hold/reused-real-frame-videos-20260519/`, leaving only Dog Staring visible as production-ready. Rex spawned background subagent `dog-content-unique-frame-completion` and added cron job `2c0e3836-e024-4160-a5a2-27f82e1cf43a` every 30 minutes to continue/verify unique-frame completion without owner input until `npm run dog:production` passes for all four videos.
- Masala approved the Dog Content examples and asked to also make Dog Staring match the newer perfect workflow. Rex rerendered Dog Staring through `render_dog_audio_truth_video.mjs` using `visual_plan_v2_132.json` + final voiceover alignment, producing a clean master with `mov_text` soft subtitle track at `vibe-zone/media/practice/youtube-automation/dog-content/dog-staring-psychology/dog-staring-audio-truth-captioned-v2.mp4` and Telegram-visible review cut `.../dog-staring-psychology/previews/dog-staring-preview-1min-telegram-visible-captions.mp4`. Verification: master streams include h264 video + aac audio + mov_text subtitle; `npm run dog:production` passes all four current Dog Content videos.
- Final Dog Content batch state at compaction: all 4 current videos (Dog Staring, Follow Everywhere, Zoomies, Guilty Look) are considered complete by the production gate. The agreed workflow is: clean production master should keep subtitles as a timed subtitle track, but Telegram review cuts need visible burned-in captions because Telegram strips/ignores soft subtitles. Masala approved the examples and asked for Dog Staring consistency, which was completed. Next CTA-chain video to make is `Why Dogs Bring You Toys/Gifts` when the next loop starts.
- Masala reviewed Dog Staring and reported the beats/visuals looked mixed up against the actual script. Rex confirmed via contact-sheet visual QA: audio/captions were aligned, but Dog Staring frames read like a shuffled pool of dog-staring scenes with inconsistent owner/dog identity and random off-script props/characters/settings. Rex wrote `youtube-automation/projects/dog-staring-psychology/full_video_prep/VISUAL_QA_FAILURE.md`, copied the current master to `vibe-zone/_visual_qa_hold/dog-staring-jumbled-beats-20260519/`, spawned subagent `dog-staring-visual-beat-rebuild`, and added cron job `034a5626-81b4-447a-b765-26937e805c85` every 30 minutes to continue rebuilding Dog Staring's visual beat layer without owner input until contact-sheet QA and `npm run dog:production` pass. Rebuild requirements: use `visual_plan_v2_132.json` beat order, generate real OpenAI frames beat-by-beat from script context, maintain consistent golden retriever + woman owner continuity, and no random babies/men/vet/crowds/wolf/laundry/robot-vacuum/puzzle-board/night drama unless the exact beat calls for it.
- Masala asked to lock in and confirm the full Dog YouTube Automation flow so the same system can be kicked off for another niche with a different SOP. Rex created `youtube-automation/docs/YOUTUBE_AUTOMATION_NICHE_FLOW.md`, a reusable locked spec separating niche-specific layers (SOP, research rules, CTA chain, visual/thumbnail style bible, production gate names) from universal gates (SOP, visual plan, real image-gen frame gate, final-audio word timestamps, audio-truth render, contact-sheet visual progression QA, review slot hygiene, thumbnails, Telegram review cuts, archive/debug hygiene). It explicitly records the Telegram behavior: production masters keep clean video + soft subtitle track; Telegram review cuts burn visible captions because Telegram strips/ignores soft subtitles.
- Masala confirmed the master YouTube Automation template: 1) SOP/script gate using specified SOP docs, 2) visual-plan gate, 3) real image-gen frame gate, 4) final audio + Whisper word timestamps, 5) audio-truth render, 6) contact-sheet visual progression QA, 7) clean review-slot gate, 8) thumbnail gate, 9) Telegram 1-min review cut, 10) archive/debug hygiene. He wants each niche saved separately so SOPs never cross-contaminate. Rex updated the standalone repo with `templates/MASTER_YOUTUBE_AUTOMATION_FLOW.md`, `niches/dog-psychology-automation/` containing Dog SOP/reference docs, and `niches/the-experiment-where/` reserved for the next niche SOP ingestion. Pushed to GitHub repo `mixmasterfunk900-cmyk/-youtube-automation-niche-flow` after using a new deploy key.
- Masala provided four SOP docs for the new niche `The Experiment Where` and explicitly warned not to confuse them with Dog SOPs. Rex saved them only under `youtube-automation-niche-flow-repo/niches/the-experiment-where/sop/`, scaffolded the first project `the-experiment-where-children-were-taught-to-fear`, wrote source notes, a 1223-word script `The Experiment Where Children Were Taught to Fear`, a 40-beat draft visual plan, and generated 4 real OpenAI thumbnail options. Rex added `vibe-zone/scripts/check-experiment-where-sop.mjs` plus npm scripts `experiment:sop` and `experiment:production`; `npm run experiment:sop`, Vibe lint, and build passed. The standalone repo was pushed with SOPs and first script/plan commit `2d97be4`. Rex spawned background frame production `experiment-where-children-fear-frame-production` to generate draft frames; final audio-truth video is blocked until final voiceover audio exists.
- Masala caught that The Experiment Where draft was only 1223 words and 40 beats, while his intended SOP target is 2,000+ words with many more beats. Rex confirmed the shorter count came from the provided SOP text saying 950–1,400 words, but Masala's latest correction overrides it. Rex paused the old 40-beat generation, rewrote `the-experiment-where-children-were-taught-to-fear/scripts/script_v1.md` to ~2003 words, regenerated `visual_plan_draft.json` to 129 beats, updated the experiment SOP gate to require 2000–2600 words, and `npm run experiment:sop` passes. Rex spawned `experiment-where-129-beat-frame-production` and updated cron job `2916d07f-05d4-4e4a-946a-6a7cedccbcff` so all future image generation uses the corrected 129-beat plan; the old 40-beat plan is obsolete. Changes pushed to standalone repo.
- Masala asked to add the new `The Experiment Where` concept to Vibe Zone in a new pipeline. Rex refactored the Vibe Zone YouTube Automation page to support multiple SOP-isolated channel lanes instead of only hardcoded Dog Content. It now shows separate selectable pipeline cards for `Dog Psychology Automation` and `The Experiment Where`, auto-discovers assets under `media/practice/youtube-automation/the-experiment-where/<project>/`, and displays scripts/SOP assets, thumbnails, visual frames, and videos per selected niche. Build/lint passed and `graphify update .` refreshed the Vibe Zone graph.
- Masala reported Vibe Zone YouTube Automation detail formatting was broken in both pipelines: thumbnail ideas were only text links and long script/visual-plan asset names bled into the neighboring column. Rex updated `src/App.tsx` to render thumbnail and hook/visual-frame assets as preview grids with actual image/video thumbnails, and appended CSS fixes in `src/App.css` adding min-width guards, ellipsis truncation, and media-card grids. Verification passed: Vibe `npm run lint`, `npm run build`, and `graphify update .`.
- Masala asked for Vibe Zone thumbnail previews to be clickable without auto-downloading, opening fullscreen with arrow navigation. Rex added a YouTube Automation lightbox/gallery in `src/App.tsx` for image preview cards, with click-to-open, previous/next buttons, keyboard ArrowLeft/ArrowRight support, Escape close, and no `download` behavior for media cards. Added fullscreen lightbox styling in `src/App.css`. Verification passed: Vibe `npm run lint`, `npm run build`, and `graphify update .`.
- Masala provided the final Google Drive voiceover for `The Experiment Where Children Were Taught to Fear`. Rex downloaded it to `youtube-automation/projects/the-experiment-where-children-were-taught-to-fear/voiceover_drop/experiment-where-children-taught-to-fear-final-audio.wav` (WAV, ~13:15), transcribed it locally with Whisper base + word timestamps to `transcription/experiment-where-children-taught-to-fear-final-audio.json`, confirmed all 129 real frames were present and passed the frame gate, symlinked `approved_frames` to `approved_frames_draft`, and rendered the audio-truth master: `full_video_prep/the-experiment-where-children-taught-to-fear-audio-truth-captioned.mp4`, copied to Vibe at `vibe-zone/media/practice/youtube-automation/the-experiment-where/the-experiment-where-children-were-taught-to-fear/the-experiment-where-children-taught-to-fear-audio-truth-captioned.mp4`. Audit: 129 beats, 387 captions, planWords 2001, audioWords 1994, wordRatio 1.0035, exact-token-walk timing, avgBeatAlignmentScore 0.9787, weakBeatMatches 5, duration 795.35s, soft `mov_text` subtitle track. Rex also created the Telegram-visible 1-min burned-caption preview at `.../previews/the-experiment-where-children-taught-to-fear-preview-1min-telegram-visible-captions.mp4`, copied contact sheets/proof frames to Vibe, and verified `npm run experiment:sop` + `npm run build` passed.

## Vibe Zone site-visible captions fix
- Fixed Vibe Zone review playback so YouTube automation selected videos prefer full-length burned-caption files named `*-site-visible-captions.mp4` before soft-subtitle masters or previews.
- Rendered full-length burned-caption review copies for Dog Staring, Why Dogs Follow You Everywhere, Why Dogs Get Zoomies, Why Dogs Look Guilty, and The Experiment Where Children Were Taught To Fear.
- Preserved clean production masters separately; browser/Vibe review files now use captions burned into the video because `mov_text` soft subtitles are not reliable in browser playback.
- Verified timing coverage from audio-truth ASS files: all caption tracks start at 0.00s, end within the MP4 duration, and use the audio-truth one-line caption files. Proof contact sheet: `vibe-zone/media/proof/site-visible-caption-proof-known-caption-times-20260519.jpg`.

## Pre-compaction flush — Vibe Zone caption visibility fix
- User reported Vibe Zone website videos were missing captions / captions not showing in browser playback.
- Diagnosis: site/browser playback was using clean MP4s with soft `mov_text` subtitle tracks, which are unreliable/invisible in HTML video players.
- Fix applied: generated full-length burned-in caption review copies named `*-site-visible-captions.mp4` for all current Vibe-visible YouTube automation videos while preserving clean production masters separately.
- Vibe Zone `src/App.tsx` was updated so selected review videos prefer, in order: full-length `site-visible-captions.mp4`, then 1-minute `visible-captions` preview cuts, then older audio-truth/practice fallbacks.
- Burned-caption site-visible videos completed for: Dog Staring, Why Dogs Follow You Everywhere, Why Dogs Get Zoomies, Why Dogs Look Guilty, and The Experiment Where Children Were Taught to Fear.
- Verification completed before compaction: all 5 site-visible videos exist in their Vibe media folders, have video+audio streams, captions use audio-truth timing, subtitle events start at 0.00s and end within MP4 duration, and sampled frames at known caption-event timestamps showed captions visibly burned in.
- Proof artifact: `vibe-zone/media/proof/site-visible-caption-proof-known-caption-times-20260519.jpg`.
- Vibe Zone `npm run lint` and `npm run build` passed after UI preference changes.
- User-facing note already sent: hard refresh Vibe Zone if page is already open so it picks up the new files.

## Dog Content pause — SOP/style drift (2026-05-19)
- Masala said to pause the dog video work because each video is moving away from the agreed Dog Content SOP image style and thumbnail style, and the drift is painful.
- Rex disabled cron job `034a5626-81b4-447a-b765-26937e805c85` (Dog Staring visual beat rebuild follow-up). No further Dog Staring/dog video production should run until the image style bible + thumbnail style are re-locked and enforced by a stronger QA gate.
- Durable dog image style reminder from memory: premium original hand-drawn educational explainer, warm cream paper texture, charcoal linework, soft watercolor grain, golden dog/beagle, terracotta/coral owner sweater, no readable generated text/logos/watermarks/arrows, YouTube 16:9.

## Dog Content SOP drift root-cause audit + gate fix
- Paused Dog Content remained correct. Audit found Zoomies and Guilty had actual contact-sheet/multi-panel images inside `approved_frames/`, not just bad review UI.
- Root cause: multi-beat/batch image generations (source names like `zoomies-unique-batch-001-004`) were accepted as beat frames. Old gate only checked file count/size/provider/reuse, so real OpenAI images passed even when they were 2x2 contact sheets, off-style, or thumbnail/contact-sheet concepts.
- Added locked Dog style bible: `youtube-automation/docs/DOG_CONTENT_STYLE_BIBLE.md`.
- Added hard visual SOP gate: `youtube-automation/scripts/check_dog_visual_sop.mjs` to block non-16:9 frames, contact-sheet/multi-panel frames, missing `styleContract: dog-psychology-v1`, batch-pattern source files, missing per-beat provenance, missing/failing `visual_sop_audit.json`, and missing/failing `thumbnail_sop_audit.json`.
- Patched `youtube-automation/scripts/render_dog_audio_truth_video.mjs` so Dog renders/copies are blocked unless the new visual SOP gate passes.
- Patched `vibe-zone/scripts/check-dog-production-ready.mjs` so Dog production readiness runs the visual SOP gate across all current Dog videos.
- Wrote fail audits for current Dog videos and thumbnails. Verification: `npm run dog:production` now intentionally fails all current Dog videos with `visualSopGate=fail`; Zoomies reports 52 likely contact-sheet/multi-panel frames and Guilty reports 46. Root-cause report: `youtube-automation/audit/dog-sop-style-20260519/ROOT_CAUSE_AND_FIX_REPORT.md`.

## Dog Content SOP drift root-cause + gate fixes (late 2026-05-19)
- Masala complained that Zoomies/Guilty Dog Content videos looked like the attached bad example: contact-sheet / multi-panel generated images inside the video, drifting far from the agreed Dog SOP and thumbnail style.
- Audited Dog Content current videos against the agreed style: premium original hand-drawn educational explainer, warm cream paper texture, charcoal linework, soft watercolor grain, golden dog/beagle, terracotta/coral owner sweater, no readable generated text/logos/watermarks/arrows, YouTube 16:9.
- Findings:
  - `why-dogs-get-zoomies`: HARD FAIL; 52 likely multi-panel/contact-sheet frames in `approved_frames`; example `beat_043.png` is a 2x2/contact-sheet artifact. Manifest source names like `zoomies-unique-batch-001-004` indicate batch/multi-beat generations leaked into final frames.
  - `why-dogs-look-guilty`: HARD FAIL; 46 likely multi-panel/contact-sheet frames plus style/continuity drift and batch-pattern provenance.
  - `why-dogs-follow-you-everywhere`: closest to acceptable, but still blocked; lacks visual SOP audit/style contract/per-beat provenance and thumbnails are wrong style/contact-sheet/photoreal options.
  - `dog-staring-psychology`: fail/hold; no grid artifact but has 29 non-16:9 frames and known dog/style/progression drift.
- Root cause: old gate `youtube-automation/scripts/check_dog_frame_quality.mjs` only checked file count, file size, provider/model, and reuse; it proved frames were real generated files but not that they were one coherent SOP-matching scene per beat. `approved_frames/` was treated as approval without a blocking visual/style audit. Thumbnail workflow had same issue.
- Fixes applied:
  - Added locked Dog style bible: `youtube-automation/docs/DOG_CONTENT_STYLE_BIBLE.md`.
  - Added hard visual SOP gate: `youtube-automation/scripts/check_dog_visual_sop.mjs`.
  - Gate blocks non-16:9 frames, likely 2x2/contact-sheet/multi-panel frames, batch-pattern source files, missing `styleContract: dog-psychology-v1`, missing per-beat provenance, missing/failing `visual_sop_audit.json`, and missing/failing `thumbnail_sop_audit.json` when thumbnails exist.
  - Patched `youtube-automation/scripts/render_dog_audio_truth_video.mjs` to run the new visual SOP gate before rendering/copying.
  - Patched `vibe-zone/scripts/check-dog-production-ready.mjs` to run the new visual SOP gate across Dog projects.
  - Added report: `youtube-automation/audit/dog-sop-style-20260519/ROOT_CAUSE_AND_FIX_REPORT.md`.
- Verification/result: `npm run dog:production` now correctly fails all current Dog videos at `visualSopGate=fail`; this is desired until Dog frames/thumbnails are rebuilt through the locked style bible.
- Current status: Dog Content production remains paused/blocked. Do not continue generating/rendering Dog Content until visuals/thumbnails are regenerated as one single 16:9 SOP-matching story frame per beat, manifests include `styleContract: dog-psychology-v1` and per-beat provenance, visual SOP audit passes, and thumbnail SOP audit passes.
