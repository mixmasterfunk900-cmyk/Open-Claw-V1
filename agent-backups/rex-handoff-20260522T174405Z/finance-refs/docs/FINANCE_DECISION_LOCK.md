# Finance Decision Lock — Laura & John's Money Gap

Date locked: 2026-05-20

These decisions answer the initial SOP review blockers and apply to future Finance niche video requests unless Masala overrides them.

## 1. Image generation
Use the GPT/frontier image model linked to OpenClaw for generation. The SOP's `ChatGPT Images 2.0` label means the OpenClaw-linked frontier GPT image generation path, not a cheaper/lower-tier fallback.

If the frontier image path fails, log the failure and repair path. Do not silently downgrade without recording why.

## 2. Upload destination
Completed QA-passed Finance videos should be added/uploaded to Vibe Zone automatically.

This does **not** by itself mean public YouTube publishing. Keep YouTube upload conservative unless Masala later explicitly enables automatic YouTube private upload for this Finance lane.

## 3. Drive archive
Masala allows Finance outputs/assets to be added to this Drive folder:

`https://drive.google.com/drive/folders/10hjKYn8QrwM2K9w1rs1vdP1VFdmFwt-y?usp=sharing`

Folder ID:

`10hjKYn8QrwM2K9w1rs1vdP1VFdmFwt-y`

If Drive upload has credentials/access/API issues, fall back to local saves and mark Drive as blocked/fallback in the final report.

## 4. Voice lock
Auto-lock the first passing Kokoro John/Laura voice pair. Do not stop for a preview sample. If Masala dislikes the final output, he will say so and the voice lock can be revised.

## 5. Length
Minimum valid Finance video length is 8 minutes. Anything over 8 minutes is acceptable if the content, pacing, and QA pass. The SOP's ~20 minute target remains a target, not a hard minimum.
